# Coding_Dojo

Cohort Location: Burbank, CA <br>
Start Date: September 16th, 2019 <br>
End Date: December 20th, 2019

## Repository maintenance

This is an archive of bootcamp coursework. Build artifacts that previously
triggered GitHub warnings have been removed from version control:

- Committed `node_modules/` and `package-lock.json` files (use `npm install`
  to regenerate per project).
- Oversized `.zip` archives that exceeded GitHub's file-size limit.

A `.gitignore` is in place to keep these out of the repository going forward.
